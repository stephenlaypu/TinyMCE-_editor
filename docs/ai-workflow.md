## 重新開始新聊天視窗，輸入以下:

請以 UTF-8 編碼讀取與輸出所有檔案，不要變更檔案編碼。

請先閱讀：

- AGENTS.md
- docs/project-spec.md

若本次工作與編輯器相關，再閱讀：

- docs/editor-experience.md

若本次工作涉及產品規劃、功能設計或需求決策，再閱讀：

- docs/vision.md

閱讀完成後，請以這些文件作為本次工作的專案知識來源。

若文件與目前程式碼衝突：

- 以目前程式碼為準
- 不要自行猜測不存在的功能
- 若發現文件已過時，可於工作完成後一併更新

理解後再開始執行後續任務。

開始。

然後再接：

請實作 XXX

或

請修改 XXX


## 每次功能完成，輸入以下:

請以 UTF-8 編碼讀取與輸出所有檔案，不要變更檔案編碼。

本次功能已完成且驗證通過。

請同步更新本次修改所影響的文件。

規則：

- 僅修改受本次功能影響的文件
- 以目前程式碼為準
- 刪除過時內容
- 不保留已淘汰方案
- 不新增 Changelog（變更紀錄）
- 不產生分析報告
- 必要時可重新整理章節結構
- 不要僅向下追加內容
- 若文件與程式碼不一致，以目前實作為準

## 重大改動，輸入以下:

請以 UTF-8 編碼讀取與輸出所有檔案，不要變更檔案編碼。

請重新同步整個專案文件。

請閱讀：

- AGENTS.md
- docs/project-spec.md
- docs/editor-experience.md
- docs/vision.md

規則：

- 以目前程式碼為主要依據
- 保留仍有效的設計決策
- 刪除過時內容
- 刪除不存在的功能描述
- 刪除已淘汰方案
- 必要時可重新調整章節結構
- 不新增 Changelog（變更紀錄）
- 不產生分析報告
- 文件應反映目前專案狀態，而非保留歷史版本

## Bug 修復:

請以 UTF-8 編碼讀取與輸出所有檔案，不要變更檔案編碼。

請修正以下問題。

修改原則：

- 優先維持既有架構
- 不修改無關程式碼
- 不進行額外重構
- 不調整程式碼風格
- 僅修改修復此問題所需的最小範圍
- 完成後確認沒有影響既有功能

問題：

XXX

# 新功能開發:

請以 UTF-8 編碼讀取與輸出所有檔案，不要變更檔案編碼。

請實作以下功能。

修改原則：

- 符合目前專案架構
- 優先沿用既有元件與工具
- 保持程式碼風格一致
- 不修改無關功能
- 完成後同步更新受影響文件

需求：

XXX